package controller.commands;

import controller.Command;
import controller.GameController;

public class DropCommand implements Command {
  public DropCommand(GameController controller, Object p1) {
  }
}
