package controller.commands;

import controller.Command;
import controller.GameController;

public class AnswerCommand implements Command {
  public AnswerCommand(GameController controller, Object p1) {
  }
}
