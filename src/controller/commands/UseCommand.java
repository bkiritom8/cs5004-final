package controller.commands;

import controller.Command;
import controller.GameController;

public class UseCommand implements Command {
  public UseCommand(GameController controller, Object p1) {
  }
}
