package controller.commands;

import controller.Command;
import controller.GameController;

public class ExamineCommand implements Command {
  public ExamineCommand(GameController controller, Object p1) {
  }
}
